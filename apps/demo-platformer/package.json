{
  "name": "@engine/demo-platformer",
  "type": "module",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev": "vite --host 127.0.0.1 --port 5178",
    "build": "bun run typecheck",
    "typecheck": "tsc -p ../../tsconfig.json --noEmit",
    "test": "bun test --pass-with-no-tests"
  },
  "dependencies": {
    "@engine/ecs-core": "workspace:*",
    "@engine/debugger": "workspace:*",
    "@engine/runtime": "workspace:*",
    "@engine/renderer": "workspace:*",
    "@engine/loader": "workspace:*",
    "@engine/input": "workspace:*",
    "@engine/physics": "workspace:*"
  }
}
